class Clocks {
    constructor(x, y, timezone = 'UTC',  ) {
        // x=position, y=position, timezone
        this.time = new Date();
        this.timezone = "UTC";
    }

    // Add methods and properties here
}